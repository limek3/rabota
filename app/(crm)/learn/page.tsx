"use client";

import { Academy } from "@/components/learn/Academy";

/**
 * «Академия обзвона» — раздел обучения платформы.
 * Вся логика живёт в components/learn: разделы, курсы, тесты, справочники.
 */
export default function LearnPage() {
  return <Academy />;
}

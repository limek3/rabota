-- Фото контакта.
--
-- Хранится как data URI (или внешняя ссылка): клиент перед сохранением
-- ужимает картинку до 160×160 и жмёт в JPEG, потому что аватар рисуется
-- 30–40px — отдельный бакет в storage ради кружка пока не оправдан.
-- Понадобятся оригиналы — колонка станет ссылкой на объект, тип не изменится.
alter table public.candidates
  add column if not exists avatar text;

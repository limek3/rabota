-- ════════════════════════════════════════════════════════════════════════
--  LEADUP CRM — регион лида («Основа» / «Регионы»)
--
--  Supabase → SQL Editor → вставить файл целиком → Run. Повторный запуск безопасен,
--  данные не трогаются: у старых лидов регион пустой — CRM считает их основой.
--
--  Списки городов, цена регионального лида и апрув регионов хранятся в настройках CRM
--  («Настройки → Система → Регионы»), в базе — только регион каждого лида.
--  Добавление колонки коротко блокирует таблицу leads; если запрос упал по
--  lock_timeout — просто запустите ещё раз.
-- ════════════════════════════════════════════════════════════════════════

set local lock_timeout = '10s';

alter table public.leads add column if not exists region text not null default '';

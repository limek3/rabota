-- ════════════════════════════════════════════════════════════════════════
--  LEADUP CRM — журнал изменений: «было → стало»
--
--  Supabase → SQL Editor → вставить файл целиком → Run. Повторный запуск безопасен,
--  данные не трогаются. Старые записи журнала остаются как были — без подробностей.
--
--  audit.changes — список изменённых полей: [{ "f": "Проект", "from": "Авто", "to": "Недвижимость" }].
--  Пока колонки нет, CRM пишет журнал как раньше — одной строкой.
-- ════════════════════════════════════════════════════════════════════════

alter table public.audit add column if not exists changes jsonb;
